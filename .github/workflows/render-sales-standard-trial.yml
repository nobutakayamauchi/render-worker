name: Render AI CM Render Standard Trial
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths: ['sales_ad_standard_trial.html','standard/**','assets/**','scripts/generate_sales_audio.sh','.github/workflows/render-sales-standard-trial.yml']
jobs:
  render-standard-trial:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: '22'}
      - name: Install
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg fonts-noto-cjk
          npm install -g hyperframes@0.8.19
      - name: Validate trial sources
        run: |
          test -f sales_ad_standard_trial.html
          test -f standard/SHOT_PLAN.json
          test -f standard/CONTINUITY.json
          test -f sales_ad_v3.html && test -f sales_ad_v2.html && test -f sales_ad.html && test -f index.html
          grep -q 'ai-cm-render-standard-trial' sales_ad_standard_trial.html
          grep -q 'Standard' sales_ad_standard_trial.html
          grep -q '39,800円' sales_ad_standard_trial.html
      - name: Prepare isolated trial
        run: |
          cp index.html /tmp/bridgepatch-index.html
          cp sales_ad.html /tmp/sales-v1.html
          cp sales_ad_v2.html /tmp/sales-v2.html
          cp sales_ad_v3.html /tmp/sales-v3.html
          cp sales_ad_standard_trial.html index.html
          mv sales_ad_standard_trial.html /tmp/sales-standard-trial.html
      - name: Render
        run: |
          mkdir -p renders dist
          hyperframes render --output renders/ai-cm-render-standard-trial-video.mp4 --fps 30 --quality standard --workers 2
      - name: Audio
        run: bash scripts/generate_sales_audio.sh dist
      - name: Mux
        run: ffmpeg -hide_banner -loglevel error -y -i renders/ai-cm-render-standard-trial-video.mp4 -i dist/ai_cm_render_sales_audio.wav -t 15 -map 0:v:0 -map 1:a:0 -c:v libx264 -preset medium -crf 18 -pix_fmt yuv420p -c:a aac -b:a 192k -ar 48000 -movflags +faststart dist/ai_cm_render_standard_trial.mp4
      - name: Verify
        run: |
          ffprobe -v error -show_entries stream=codec_name,width,height,r_frame_rate -show_entries format=duration -of json dist/ai_cm_render_standard_trial.mp4 | tee dist/standard-trial-probe.json
          test "$(ffprobe -v error -select_streams v:0 -show_entries stream=codec_name -of csv=p=0 dist/ai_cm_render_standard_trial.mp4)" = h264
          test "$(ffprobe -v error -select_streams a:0 -show_entries stream=codec_name -of csv=p=0 dist/ai_cm_render_standard_trial.mp4)" = aac
          test "$(ffprobe -v error -select_streams v:0 -show_entries stream=width -of csv=p=0 dist/ai_cm_render_standard_trial.mp4)" = 1080
          test "$(ffprobe -v error -select_streams v:0 -show_entries stream=height -of csv=p=0 dist/ai_cm_render_standard_trial.mp4)" = 1920
          d=$(ffprobe -v error -show_entries format=duration -of csv=p=0 dist/ai_cm_render_standard_trial.mp4); awk -v d="$d" 'BEGIN{exit !(d>=14.9 && d<=15.1)}'
      - name: Preserve baselines
        run: |
          grep -q BridgePatch /tmp/bridgepatch-index.html
          grep -q ai-cm-render-sales-v1 /tmp/sales-v1.html
          grep -q ai-cm-render-sales-v2 /tmp/sales-v2.html
          grep -q ai-cm-render-layered-v3 /tmp/sales-v3.html
      - name: Restore source
        run: cp /tmp/sales-standard-trial.html sales_ad_standard_trial.html
      - uses: actions/upload-artifact@v4
        with:
          name: ai-cm-render-standard-trial
          path: |
            dist/ai_cm_render_standard_trial.mp4
            dist/standard-trial-probe.json
            sales_ad_standard_trial.html
            standard/SHOT_PLAN.json
            standard/CONTINUITY.json
          retention-days: 30
